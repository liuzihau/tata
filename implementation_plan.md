# implementation_plan.md — consolidated

This doc has been merged into the new three-doc layout:

- **`design.md`** — research framing, problem statement, motivation, milestones.
- **`engineering.md`** — code-level contracts (cache schema, tensor shapes, forward signatures), current implementation status, infrastructure notes. **This is the file that replaces most of the old `implementation_plan.md` content.**
- **`usage.md`** — operator's manual (commands to run, error handling).

For "what does an agent need to read before working on the code,"
start with `engineering.md`. For "what is this project about,"
start with `design.md`.

This stub is kept so any external links still resolve. Safe to delete
once those links are updated.
