# improvements.md — consolidated

The work-tracker content of this file has moved to `engineering.md`,
under the per-surface-area sections (data / model / loss / training /
inference / eval / sanity tests) and the "Active improvements / open
items" list (§9). Status legend is still `[ ]` / `[~]` / `[x]` / `[!]`.

For "what's been done and what's next," see `engineering.md` §9.
For "why this project exists," see `design.md`.
For "how to run things," see `usage.md`.

This stub is kept so any external links still resolve. Safe to delete
once those links are updated.
